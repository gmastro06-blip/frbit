---
name: User profile — frbit project
description: Developer of a Tibia bot (frbit/waypoint-navigator), working in Python with background threading, CV2, Win32 API, and game automation
type: user
---

Working on a Python-based Tibia bot called frbit/waypoint-navigator. The project is production-quality with 66 source files, mypy passing cleanly, and extensive use of threading, numpy/cv2 (minimap radar), Win32 API (input simulation), and A* pathfinding. The user values thorough structural review over surface-level style comments.

Expertise signals: uses `dataclasses.replace`, `threading.Lock`, `heapq`, `functools`, frozen dataclasses with `slots=True`, protocol-based injection patterns. Code is already well-typed and documented — skip basic best-practices.

Languages/frameworks active in this project: Python 3.10+, numpy, cv2, threading, ctypes/Win32, mypy (strict-ish), optional Arduino/Pico HID serial.
